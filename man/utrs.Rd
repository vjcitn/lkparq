% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtf_parquet.R
\name{utrs}
\alias{utrs}
\alias{utrs,GTFParquet-method}
\alias{codons,GTFParquet-method}
\alias{codons}
\title{Extract UTR and codon features from a GTFParquet object}
\usage{
utrs(x, type = "both", filter = NULL)

\S4method{utrs}{GTFParquet}(x, type = c("both", "5prime", "3prime"), filter = NULL)

codons(x, type = "both", filter = NULL)

\S4method{codons}{GTFParquet}(x, type = c("both", "start", "stop"), filter = NULL)
}
\arguments{
\item{x}{A \code{\link{GTFParquet}} object.}

\item{type}{For \code{utrs}: one of \code{"both"}, \code{"5prime"}, or \code{"3prime"}.
For \code{codons}: one of \code{"both"}, \code{"start"}, or \code{"stop"}.}

\item{filter}{Optional named list for filtering features.}
}
\value{
A \link[GenomicRanges]{GRanges} object with metadata columns
  including \code{feature_type}, \code{transcript_id}, and \code{gene_id}.
}
\description{
Functions to extract UTR (untranslated region) and codon features
from a GTFParquet object. These features are stored in the features.parquet
file generated by gtf_to_parquet.py.
}
\examples{
\dontrun{
gtf <- GTFParquet(system.file("gc49", package="lkparq"))

# 5' UTRs
utr5 <- utrs(gtf, type = "5prime")

# 3' UTRs
utr3 <- utrs(gtf, type = "3prime")

# Start codons
start <- codons(gtf, type = "start")

# Stop codons
stop <- codons(gtf, type = "stop")
}

}
